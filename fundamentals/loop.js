/* iteradores classicos
while, do while, for
 */

/* incremento */
let count = 1;
do {
    console.log(`conte até o número chegar em ${count}`)
    count++;
} while (count <= 9)

let str = "hello"
let counts = 0  

do {
    console.log(str[counts])
    counts++     
} while (counts < str.length);

/* 
dados interaveis
for in 
for of */