const idade = 20;

idade >= 18 ? console.log("tem idade maior eim!") : console.lo("não tem idade maior :(")

const sumMult = (valor) => {
    return valor >= 10 ? valor + valor : valor * valor 
}

console.log(sumMult(10))
